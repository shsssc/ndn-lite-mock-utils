# ndn-lite-mock-utils
Mock utilities for ndn-lite testing purposes

## Files
The `devices` folder is for mocked devices together with their credentials.
The `hardware-interface` folder is a simple setg of hardware read/write writing APIs and command line utilities.

